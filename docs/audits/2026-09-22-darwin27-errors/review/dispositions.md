# Review and CI follow-up

Initial RAS run `20260922T212455-6f98b1776ed75cad3a04ffdc` reviewed head `9f162e44e15c9e2d742b326d661422a3a280d379`. Claude Fable, Claude Opus and Grok completed review and adjudication; both Codex reviewer processes and the Codex synthesis process failed. There is no synthesis judgment and this is not recorded as a completed clean RAS run. The [retained findings and statuses](initial-review.json) support independent disposition. Nothing was posted by RAS.

- **fix-now:** CLAUDEFABLE-F-001 and duplicate CLAUDEOPUS-F-001. The approved admission seam requires the shipped table to exclude Darwin 27 amd64. The existing table test replaced the production table, so it could not catch a missing constraint in the new entry. Remove the replacement and exercise its existing six cases against the real table. This is a test-only change at the existing admission seam, with no native collection or broader completeness obligation. An overlay removing only the production entry's architecture restriction now fails the 27/amd64 assertion; the unmodified tests pass. This is one invariant/owner and one semantic counterexample, not a repeated-root stop.
- **reject as required work:** CLAUDEFABLE-F-002. The frozen binary README preserves the original extraction command; the maintained results record already supplies the correct copy-to-scratch reproduction instructions. No evidence is missing or invalid, and changing a frozen README plus its digest is unnecessary verification-aid polish. No follow-up ticket is warranted.

CI run [35786495037](https://github.com/the-sarge/quic-go-fast/actions/runs/35786495037/job/106944450136) failed only `TestReadECNFlagsDualStack` at its IPv4 one-second receive wait on the GitHub-hosted macOS Go 1.26 runner. The job sets TIMESCALE_FACTOR=10, but this test's two waits did not use it. The same head's other 32 checks, including the push macOS Go 1.26 job, passed. The log does not distinguish a delayed packet from a dropped packet; no Darwin 27 batching regression is inferred from it and no unchanged-head rerun was requested.

A bounded diagnostic overlay delayed delivery from the existing receiver helper to its test channel by 1250 ms after a successful real receive. With TIMESCALE_FACTOR=10, the old test still timed out after one second. Making the two existing dual-stack waits use `scaleDuration(time.Second)` let the delayed IPv4 and IPv6 observations pass, and the ordinary test passed without the overlay. This establishes and repairs the test's failure to honor the configured runner tolerance, without claiming to reconstruct the original runner's scheduling. The only CI repair is those two test waits; no product packet path changes. Raw diagnostic outputs are retained alongside this record. The native qualification captures remain unchanged.

The remaining review uses the functioning configured reviewers and Claude Opus synthesis through a task-local configuration, preserving global settings. Verification is attempted against the accepted source findings; if the missing synthesis prevents verification, retain that tool failure and carry the independently verified finding into the single allowed replacement review. The replacement must also examine the two test-wait edits. No automatic fixer is used.
