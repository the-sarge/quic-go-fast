> *This was generated by AI during triage.*

## Agent Brief

**Category:** bug

**Summary:** Diagnose the observed Ubuntu `TestDial/Dial` transport-running assertion failure within a finite investigation budget. This brief supersedes the earlier recommendation to retain `needs-triage`: non-reproduction leaves the cause open but does not prevent an agent from undertaking a specified investigation. Triage is complete; the next work is diagnosis.

**Current behavior:**

The hosted failure occurred at commit `b1f16ae0edf3f23483e2009d8196912c39c2eac5`, Ubuntu 24.04.5, Go 1.26.8 linux/amd64, CGO enabled, non-race, `GOTOOLCHAIN=local`, `TIMESCALE_FACTOR=10`, root shuffle seed `1789428569339240250`. After cancellation returned `context.Canceled` and the caller-owned UDP socket remained open as expected, `areTransportsRunning()` still detected a `Transport.listen` stack. This process-wide Boolean does not identify the responsible transport or establish a leak, shutdown race, observer discrepancy, or infrastructure fault.

At the inspected revision, `Dial` calls `Transport.Close` on failure; for caller-owned sockets, that method waits for the receive loop's completion channel while preserving the socket. The relevant implementation and test code remained unchanged through main `4fcf07fd9836a6dafebfde7bbbf21fef436d2dc7`.

Two local attempts using the exact failing source, toolchain and root seed passed: a seeded suite and a focused `TestDial` run. Both used coverage and time scale 10. They ran in an Ubuntu 24.04.4 amd64 container emulated on an ARM Mac with LinuxKit 7.0.12, so they do not reproduce the hosted environment. Complete commands, results, limitations and evidence hashes are retained in the [verification comment](https://github.com/the-sarge/quic-go-fast/issues/317#issuecomment-5672631963). Do not repeat triage or treat these passes as resolution.

**Desired behavior:**

Produce an evidence-backed diagnosis identifying the transport and the relevant cancellation/shutdown/observation boundary, or a bounded non-reproduction report with a concrete future-failure capture proposal. Preserve public API, wire, caller-owned socket and incoming-storage lifetime contracts. Any proposed repair must follow from observed evidence and remain a separate implementation scope.

**Key interfaces:**

- `TestDial` / `testDial` — real UDP receipt, cancellation result and caller-owned socket assertions.
- `Dial`, `Transport.Close` and `Transport.listen` — transport identity, socket ownership, receive-loop termination and shutdown completion.
- `areTransportsRunning()` and the goroutine profile — the exact snapshot that produces the failing Boolean and its matching stacks.
- The root test suite's fixture cleanup — attribution of any observed transport to its creating test or caller.

**Acceptance criteria:**

- [ ] Reconcile relevant changes since the failing commit and preserve the existing hosted/local evidence; record source SHA, platform, toolchain, architecture/emulation, seed, time scale, commands and exit statuses for every new attempt.
- [ ] Use at most one additional seeded suite attempt and three targeted experiments, each with a declared purpose and an outer wall-clock limit of ten minutes. Prefer an already available native Ubuntu amd64 environment; explicitly record any mismatch. Stop earlier when the cause is established, the budget is exhausted, or necessary access is unavailable. Do not provision infrastructure or silently expand the run budget.
- [ ] Where diagnostic instrumentation is used, preserve the assertion's existing behavior and capture the same goroutine profile that triggers it. Correlate matching stacks with transport identity/socket address and receive-loop exit, completion-channel closure and `Close` return, as feasible. Distinguish direct observations from assumptions, and document instrumentation's scheduling effects.
- [ ] If reproduced, minimize this specific transport-observation failure and test falsifiable explanations one variable at a time. Identify causal evidence and an appropriate regression-test seam before proposing a correction.
- [ ] If not reproduced, report all attempts and limitations, the unresolved alternatives, and the smallest concrete capture needed on a future occurrence. Non-reproduction is an acceptable investigation result; it does not establish a fix or justify closing the bug as resolved.
- [ ] Post a diagnosis report, preserve logs and diagnostic patches as evidence, and remove temporary instrumentation from the working source. State the proposed next scope or required environment precisely. Completion of this brief requires no further triage interview.

**Out of scope:**

- Product or fixture repairs, timeout increases, assertion weakening, quarantine, CI policy changes, and permanent diagnostics deployment.
- Rerunning the unchanged failed hosted check, unbounded stress campaigns, or reopening the journal merge decision.
- Combining #241's owned-socket rebind failure or HTTP investigations #151/#169/#301 with this issue without evidence of a common cause.

**Readiness:** `ready-for-agent` applies to the bounded diagnosis above. It does not claim that the cause or repair is already known.
