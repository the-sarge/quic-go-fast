# Issue 618 investigation contract

The [accepted agent brief](https://github.com/the-sarge/quic-go-fast/issues/618#issuecomment-5820598213) delegates investigation decisions with advance acceptance of recommendations. The [current-source addendum](https://github.com/the-sarge/quic-go-fast/issues/618#issuecomment-5823109493) includes PTO confirmation without increasing scope. Investigation start: 2026-09-24 22:24:21 UTC; report deadline: 2026-09-25 02:24:21 UTC. Baseline source: `f47e09e3a5b4f9f8efc5de0ea73a01307d3a01f4`.

## One PR and review contract

Publish an evidence-only PR containing this contract, a local diagnostic report, exact commands and host/tool/source identity, raw results, profiles and frozen scratch source. Finish with a keep, separately scoped change recommendation, or inconclusive disposition. No production optimization ships. The unchanged recovery implementation owns semantics, the wire parser owns ACK representation, and the scratch fixture owns only the six explicit synthetic traces. Guarantee: example-level local measurements, not protocol completeness, deployed load, public activation or native qualification.

The approved final blast radius is this audit directory. Temporary package-local scratch files may construct, snapshot and restore valid fixtures and expose snapshot helpers for the existing Reno controller and atomic statistics; archive them outside the Go build as frozen evidence and remove them before committing. Snapshot restoration must restore every object mutated by the measured ACK in these fixtures. Setup, restoration, validation, formatting and cumulative recording are excluded from timed work. Setup touches caches and can cause later GC effects; report those limits rather than claiming deployment-equivalent timing.

Artifacts: scratch fixtures, raw measurements and profiles are verification aids retained as immutable historical evidence; the contract, report and receipts are process/traceability metadata. No shipped behavior or required safety enforcement changes. No maintained-aid exception is granted, and this evidence cannot gate unrelated shipped work. Contract closure is not triggered: no runtime enforcement changes or universal safety guarantee is requested. No semantic matrix, guard mutation or recursive harness proof is required.

Preserve the 32,768-entry cap, ACK-only gaps, cross-space ordering, measured-at-send eligibility, missing/disposed/evicted-history conservatism, ending-ordinal deduplication, exact spurious membership, current-path receipt authority, MTU/path-probe distinction, and path/Retry resets. Preserve Reno, public API, wire behavior, ownership and dependencies. A candidate must pass the eight existing T5 recovery functions and affected-package tests; reject it upon semantic failure. Add only a focused regression if its changed invariant cannot be distinguished by existing coverage.

## Finite execution and evidence

Exactly six cells: 1,024 / 8,192 / 32,768 retained outcomes crossed with one contiguous / 32 disjoint ACK ranges, single connection, single worker, rich event path active, ECN disabled. Pin the trace before capture: sequential legal 1-RTT packet numbers `0..N-1`, with `0..N-2` registered and ACKed through recovery in batches of at most 32 before registering the last packet. Every packet is a 40-byte ack-eliciting PING. The measured contiguous ACK covers `[N-32,N-1]`; fragmented ACKs cover 32 singleton numbers `floor(j*(N-1)/31)` for `j=0..31`, descending on the wire. Both shapes acknowledge exactly one new ack-eliciting packet. Build history through actual registrations and validated ACKs; snapshot the valid pre-ACK fixture and restore it outside timed calls. Verify occupancy, fresh ACK feedback, RTT measurement, nonempty receipt witness and positive PTO so ACK classification, PTO confirmation and persistent-span evaluation execute.

At most four working hours through the report and 30 cumulative measurement minutes including warm-up, timed fixture setup and profiling. Initial baseline: three unprofiled samples per cell, each targeting two seconds of measured ACK calls. Tail: retain at most 10,000 individual durations per cell from unprofiled calls, with counts and one timer-overhead check per implementation. Allocation deltas surround only ACK calls outside elapsed timing, with their overhead disclosed. One CPU profile, at most 10 seconds, for the highest-mean cell; label ACK work separately from reset/setup. No retries, new hosts, extra platforms, network campaign, parameter search or sample expansion. Stop at the budget with a partial/inconclusive receipt if necessary.

Mean above 10 microseconds or p99 above 100 microseconds is a diagnostic trigger, not an SLO. Only a valid exceedance attributable materially to an outcome scan permits one disposable candidate (bounded per-space lookup or classified-prefix, not both). Comparison remains optional; do not ship it. Freeze a candidate before at most three alternating baseline/candidate pairs per cell, each targeting two seconds; quantify bounded memory and registration/update costs. Recommend a production follow-up only if evidence supports it; uncertainty is an accepted result.

Terminating evidence: all six cells or explicit budget stop, timer overhead, raw sample counts and allocations, variation, bounded attribution profile, trace/source/host/tool identity, correctness/fixture checks and a stated disposition with limitations. Baseline fixture checks plus affected-package tests, `go vet ./internal/ackhandler ./internal/congestion` and `go mod tidy -diff` cover temporary Go changes. No additional race, stress, platform or timing gates are required locally.

## Selected conditional comparison

The initial capture exceeded the mean trigger at 8,192 and 32,768 outcomes, and its bounded profile attributes material CPU cost to recovery ACK lookup. Select exactly one candidate: count the decoded ACK cardinality with a saturating count-sized budget, use the existing `congestionPacketKey` map for at most `count` packet lookups when narrow, and retain the original ring scan when wider. This adds zero stored entries, no registration/update work and no new lifetime owner. Source correctness tests cover existing eviction/disposal/reset behavior; production use across different ACK densities remains outside these six measurements.

Retain the initial capture and its scratch version unchanged. Static checking found copied atomic-bearing structs in its single-worker snapshot helper. The corrected helper uses atomic loads/stores, and both paired binaries use exactly that corrected helper. Paired baselines, not differences between helper versions, own comparative conclusions. Do not repeat the initial samples or their tail capture. Candidate tails are limited to 3,333 values per sample (9,999 per cell), with one candidate timer check and one highest-mean-cell profile. Run exactly three pairs per cell in baseline/candidate, candidate/baseline, baseline/candidate order. Comparison execution has a 20-minute wall cap, subordinate to the total 30-minute measurement cap; retain all executions without retries.

## Review, certification and tracking

Review context consists of this current contract, verbatim issue acceptance criteria, the report and scratch diff, relevant source and unresolved history only. Budget: one fully briefed initial RAS review and at most one replacement following accepted fixes, with source verification as needed. Independently disposition findings; RAS never fixes. Stop for a representation mismatch, precise repeated root, material scope expansion or an accepted obligation outside the approved budget. Cheap docs-only corrections use the shared polish exemption.

Certify the exact pushed head with clean-tree and diff checks, frozen-evidence checksums, relative-link checks and the recorded focused validation. Follow `docs/REVIEW-LOOP.md`: existing workflows run on drafts; there is no `task preflight`, `ci.yml` or `ci-*` gate. Require applicable hosted checks on the exact head, mark ready, then squash merge with a matching head. Only after merge, use append-dev-journal without RAS, publish the durable report pointer to #618, revalidate any worthwhile deferred findings and complete OmniFocus task `jPPTOxHmY2U`. No new glossary or ADR is required.
