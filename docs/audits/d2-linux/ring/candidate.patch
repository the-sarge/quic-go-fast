diff --git a/datagram_queue.go b/datagram_queue.go
index 3f039fc1..3d299c5e 100644
--- a/datagram_queue.go
+++ b/datagram_queue.go
@@ -23 +23 @@ type datagramQueue struct {
-	rcvQueue [][]byte
+	rcvQueue ringbuffer.RingBuffer[[]byte]
@@ -96 +96 @@ func (h *datagramQueue) HandleDatagramFrame(f *wire.DatagramFrame) {
-	if len(h.rcvQueue) < maxDatagramRcvQueueLen {
+	if h.rcvQueue.Len() < maxDatagramRcvQueueLen {
@@ -99 +99 @@ func (h *datagramQueue) HandleDatagramFrame(f *wire.DatagramFrame) {
-		h.rcvQueue = append(h.rcvQueue, data)
+		h.rcvQueue.PushBack(data)
@@ -116,3 +116,2 @@ func (h *datagramQueue) Receive(ctx context.Context) ([]byte, error) {
-		if len(h.rcvQueue) > 0 {
-			data := h.rcvQueue[0]
-			h.rcvQueue = h.rcvQueue[1:]
+		if h.rcvQueue.Len() > 0 {
+			data := h.rcvQueue.PopFront()
