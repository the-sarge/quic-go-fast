# E1 independent review dispositions

RAS review `20260907T081528-7e4b074c7cec739bb0356bfc` reviewed [PR #33](https://github.com/the-sarge/quic-go-fast/pull/33) at `c85f2199d5fdd55ec6d8391173da8bfea208f6aa`, against parent `effb08627fdb5210087a52c24eef79c6de9e0534`. All seven reviewers completed their independent and adjudication passes, and synthesis completed. RAS did not post to GitHub. The exact E1 slice and relevant common contract were quoted mechanically in the operator brief; no positive feasibility or runtime adoption was requested.

The implementing agent independently checked each claim against the frozen sources, retained artifacts and accepted E1 scope. All accepted changes are receipt corrections: no source fixture, runtime, measurement, margin or neighboring slice contract changed. Contract closure is not triggered for these metadata corrections. There is no prior accepted fix at an invariant/owner against which a second semantic counterexample could establish a repeated-root stop.

| Cluster | Independent disposition | Evidence and current-work obligation |
| --- | --- | --- |
| C-001 | Reject | The allocation diagnostic's tag-plus-environment opt-in is intentional. Future reuse and maintained-aid completeness are outside E1. |
| C-002 | Fix now, completed | `Conn.sendPackets` reads only result `deadline`/`err`; `progress`/`stop` are unread and `Conn.run` retains `sendQueue.Available()`. The correspondence section now discloses that partial interface coverage. |
| C-003 | Fix now, completed | The frozen enum has no congestion-specific value and folds non-pacing, non-`SendAny` inner-loop outcomes into `emissionRecoveryBlocked`. The receipt now states that the conceptual distinction is not realized by the prototype result. No redesign is implied. |
| C-004 and C-012 | Fix now, completed as one disclosure | Archived counters show material pre-admission drops. The receipt now contains all cell/variant ledger means and probe p50, with units, aggregation method and scheduled-rate semantics. C-012 duplicates this same disclosure obligation. |
| C-005 | Reject | P2/P4 are separate declared cells and both results are disclosed. No universal causal comparison is claimed; another statistics narrative is not an acceptance requirement. |
| C-006 | Reject | Extraction confirms the campaign archive root has `capture.log` and `restoration.json`; the benchmark archive also has `cpu.json` and `isolation.log`. The claimed missing members exist. |
| C-007 | Fix now, completed | The host checkpoint contains topology, toolchain, service identity and the workload manifest, but no campaign-launcher content hash. The receipt now states that limitation rather than claiming a historical byte binding. |
| C-008 | Fix now, diagnosis recorded | The two failed push jobs and independently successful same-head PR integration run are described below. Their integration compile inputs are unchanged by E1. No clean retry is represented as a fix, and final product-head certification remains a separate gate. |
| C-009 | Fix now, completed | The focused fixture is absent from the frozen runtime commits. Its retained source hash, introducing product commit and copied-worktree status are now recorded; missing historical invocation/GOMAXPROCS binding is expressly disclosed. No extra measurement was taken. |
| C-010 | Fix now, completed | The frozen manifest and invocations use two processes over same-host loopback UDP. The receipt now states that domain and excludes physical-link qualification. |
| C-011 | Reject | The fixture's receive-queue setup is single-threaded; the exercised production read takes its mutex. Adding uncontended setup locks would not establish a concurrency property and is not required by the example-level characterization. |

The raw matrix and benchmark archives were extracted into empty directories. Both analyses reproduced the committed summaries exactly; archive root listings and all twelve ledger/p50 rows were checked from the extracted receipts. Current copies of the focused allocation fixture in both Linux experimental worktrees match SHA-256 `5861ea9e4d7f0aa5117191104215bf34498b894dbeee70ac0405dc3cdd92431d`; this post-capture observation does not reconstruct missing historical invocation metadata.

The shared review-loop policy permits skipping another RAS review/verification cycle after cheap, high-confidence documentation-only fixes. That policy is applied here: these corrections do not alter any runtime or fixture bytes or the inconclusive disposition. Lightweight document/artifact checks and new exact-head local certification are required. The initial review allowance is consumed; no replacement review or campaign is used. No finding requires a new follow-up issue or an operator decision.

## Hosted observations on the reviewed head

Push-triggered integration run [34099520918](https://github.com/the-sarge/quic-go-fast/actions/runs/34099520918) failed two unchanged tests. Separately triggered pull-request integration run [34099524274](https://github.com/the-sarge/quic-go-fast/actions/runs/34099524274) passed on the same head without an operator-requested rerun. Every PR-triggered workflow on that reviewed head passed; the failed push run remains a failed historical observation.

The Ubuntu Go 1.26.8 GSO-disabled job reached `handshake_drop_test.go:113` in `TestHandshakeWithPacketLoss/drop_1/3_of_packets_in_direction_both/retry:_false/server_speaks_first#03`, waiting for the client goroutine to finish in the two-minute simulated-time case. That test uses randomized packet loss. Twenty targeted repetitions on frozen base `e90617366674535bcefa8f90a2e92b153be1342b`, with Go 1.26.8, QUIC v1, GSO disabled and `TIMESCALE_FACTOR=3`, passed. Those repetitions did not reproduce the original failure and do not identify or fix its cause.

The macOS Go 1.27.1 job observed five DATA_BLOCKED frames where `TestConnDataBlocked` expected three: offsets `{100, 300, 700, 700, 700}`. Its nonfatal length assertion was followed by indexing the three-entry expected-offset slice, producing the reported panic at `packetization_test.go:270`. Twenty targeted repetitions with Go 1.27.1, QUIC v1 and `TIMESCALE_FACTOR=3` passed locally. The extra-frame cause remains unresolved; no assertion was weakened or production behavior changed.

The product diff changes only root test files and documentation/evidence. A `go list -deps -test -json ./integrationtests/self` census contained 223 repository compile inputs, none changed by this PR; module metadata is unchanged too. Root package test files are not compiled into the imported runtime used by these integration tests. The failures therefore do not demonstrate an E1 runtime regression. Under the plan's explicit docs/runtime applicability clause, these duplicate push observations are treated as unrelated to E1's changed source surface, with the successful same-head PR integration run retained as additional preservation evidence. The applicable ordinary characterization, platform, static and final exact-head gates still have to pass; this disposition does not relabel failed checks as successful or authorize a green-seeking rerun.

The final pushed head and its local/hosted certification are recorded on PR #33. No production handshake or flow-control correction is included in this evidence-only slice.
