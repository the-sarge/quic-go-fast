# Datapath Offload Program Handoff Receipt — 2026-09-11

Audit metadata for program QGF-DP-2026-09. The normative contracts are the [program index](../../adr/2026-09-11-datapath-offload-program.md) and the [G](../../adr/2026-09-11-linux-gro-plan.md), [W](../../adr/2026-09-11-windows-datapath-plan.md), and [D](../../adr/2026-09-11-darwin-batch-plan.md) track plans, bound by the [datapath offload plan](../../adr/2026-09-11-datapath-offload-plan.md).

## Provenance

The design was grilled interactively (platform weighting and ordering decided by the operator; remaining decisions delegated and evidence-backed), then stress-tested by RAS consideration run `20260911T153830-6b04131e270f058d3fc092d5` (two reviewers, adjudicated synthesis, ten findings) and driven to a clear blocking projection over four verification rounds; all eighteen prior finding clusters resolved. Material corrections adopted along the way: concurrency-safe coalesced-slab storage under a recorded ADR 0005 amendment; Windows foundation on standard-library message I/O; falsifiable adoption gates with predeclared statistical bounds and engagement metrics; caller-owned-socket protection; oversized-retention byte budget; production-shape Darwin self-check; Darwin kernel-major (not product-version) qualification keying. Run artifacts live in the local RAS store (`ras show 20260911T153830-6b04131e270f058d3fc092d5`).

## Slice audit

The slice graph (G1, G2, W1, W2, W3, D1, D2) was audited against the handoff gates before publication: every slice is vertical or a sanctioned inert expand step (G1), independently green, context-sized, explicitly ordered with genuine edges only (W3→G1 is the single cross-track edge; D's ordering preference is documented as non-blocking), TDD-shaped with closure classes named, and blast-radius traced with no silently accepted untraced effect. Single-owner and authority-completeness fields are recorded per slice in the track plans, transitional seams are budgeted (G1's inert machinery, closed by G2 and W3; D2's experiment path, closed by its own retirement disposition), artifacts are classified with no maintained-aid exception requested, and each universal criterion declares domain, owner, guarantee level, and terminating evidence. Existing work: no open PR or branch exists for any track; the KeibiSoft fork commit `24ecf34c` is an external MIT reference for D1 only, explicitly not a baseline. Rejected decompositions and merge alternatives are recorded per slice in the plans' Slice decision audit fields.

## Independent audit findings (pre-publication)

An independent fresh-context audit verified thirteen file:line anchors (all accurate) and returned eleven findings, all dispositioned into the plans before publication: (1, blocking) W3's silent dependency on G2's platform-neutral retention-queue wiring — resolved by reassigning the dormant queue-copy wiring and shared routing fixtures into G1, keeping W3 parallel with G2; (2) the USO/URO interaction matrix now belongs to whichever of W2/W3 merges second, stated in both contracts; (3) `basicConn` remains the platform-neutral non-OOB fallback — W1's ownership claim corrected; (4) race verification scoped to Linux CI, since Windows CI runs no race detector; (5) G2's context-budget line count corrected to ~475; (6) D2 now runs against its unmerged PR head — adopt merges, retire closes with zero residue, preserving the seven-PR count; (7) G1's `packetBuffer` release-path slab hook traced explicitly as a hot-path touch with a nil-hook preservation gate; (8) the G2/W3 closure invariant split into two singular-owner invariants (slab release-exactly-once; routing exactly-once); (9) compile-time absence for iOS/opt-out builds given a terminating mechanism (mutually exclusive active/stub symbol definitions verified by the cross-build matrix) and the skipped `GOOS=ios` cross-compile traced as a D1 deliverable; (10) the binding plan's "fourth slice" wording aligned to D2; (11) W's byte-for-byte acceptance wording weakened to the protocol cells its evidence can terminate. No finding remains open.
