# Scoped emission-construction handoff receipt

This receipt records process evidence for the [C plan](../../adr/2026-09-08-emission-construction-plan.md), added to existing QGF-AD-2026-09. It is not an implementation contract. The owner explicitly invoked architecture-handoff with OmniFocus parent fsNvXyZ3aDd after receiving the construction investigation. That invocation authorizes documentation publication/merge, issues and task mirrors; it does not dispatch implementation.

## Source and retained evidence

The [preserved investigation](investigation.md) inspected a5bb7f9145755a6c40482eda8cdde4c9a68b4dbf. The handoff baseline is 2a3d7670f4f1aae0cc7c81ce7b6e212087a46e8e; only docs changed between those revisions. Construction, emission, queue and fixtures remain byte-identical. Source anchors remain valid. Go 1.27.0 darwin/arm64 focused observations passed: 34 top-level emission/queue tests, 19 lifecycle tests, their 53-test union under race, and compile-only emission_experiment. These are prior baseline observations, not certification of future C1/C2. [Focused log](baseline-focused.jsonl), [lifecycle log](baseline-lifecycle.jsonl), [race log](baseline-race.jsonl), [tag log](baseline-tag-compile.log), [original evidence manifest](evidence-manifest.json). The manifest's original INVESTIGATION.md hash records the pre-publication artifact; investigation.md rewrites local links and adds this handoff context without claiming that old hash covers the published Markdown.

The investigation self-grilled complete construction, explicit final binding and minimal queue creation at the existing binder. It traced production/test construction, publication, token first use, pre-worker handshake, failure returns, path replacement/rebinding, direct probes and close. It retained recovery's live slot separately from packer's captured manager. No initialization defect or new performance claim resulted. The initial design recommendation of two small slices becomes C1/C2; the normative plan narrows optional startup testing to one StartHandshake-error worker observation, not a new early-error cleanup obligation.

## Existing work and scope decisions

GitHub had no open PRs at this scoped audit. No existing construction child issue or unmerged implementation was found. Old experimental/local worktrees are neither reused nor dependencies. Retain shipped Track E and its closed budgets. Existing T1 #100 already owns broad maintained send-entrypoint migration; C1 covers constructor-owned setup that rebuilt packer fixtures cannot establish. No T1 → C blocker is justified. Preserve all other H/I/P/T plans and issue identities.

I8 #98 already owns failed Initial publication, including the server collision concern noted as unreproduced in the investigation; do not duplicate it. The pre-worker cleanup concern is outside C and neighboring I ownership must not be constrained by a test asserting that cleanup remains absent. #103 records a subsequent pacing-test CI failure on a docs-only journal change; its prior user-directed admin merge is not authority to ignore any new failing gate.

This program was already created under the current bounded artifact/representation rules, so no legacy-program rebaseline is needed. The scoped disposition is: reopen only initial construction from the old no-code outcome; keep broad path/readiness deferrals; retain the four other current tracks; add C1 → C2. No new domain term or ADR decision is required. No repeated-root conclusion is asserted: construction ordering, path authority, startup cleanup and pacing timing have distinct invariants/owners, not repeated failures of one central fix.

## Slice audit and publication

Pre-commit independent audit: PASS. The read-only auditor checked C1/C2 against pinned source and found no fix-now or stop-for-decision finding. Real Initial AEAD and existing retransmission producers make constructor-owned output feasible without replacing the packer. The four-cell budget, maintained-aid contract, finite source guarantees, unchanged ownership, context sizes and C1 → C2 edge passed. T1 remains independent. Splitting C1 would duplicate a small test contract; merging it with C2 would discard a useful independent baseline; splitting C2 would retain unnecessary staging. No implementation or diagnostic code was written. The proposed C1 maintained-aid outcome declares payoff/domain/owner/retirement and four representative cells; C2 remains four setup symbols with finite source/representation guarantees and no packet-time change. The existing T1/other tracks are context only, not reopened contracts.

Local docs certification covers scope, source anchors, repository links, soft-wrap formatting, `git diff --check`, and runtime/module/workflow identity. A fully briefed docs PR review and actual same-head inherited hosted checks precede guarded squash merge. Issues and OmniFocus mirrors follow verification that the exact accepted plan tree is reachable on default. Final PR/head/check/merge receipts belong in the PR and publication report, not duplicated normative slices. No implementation agent is launched.
