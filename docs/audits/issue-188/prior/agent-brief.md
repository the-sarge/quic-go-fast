> *This was generated by AI during triage.*

## Agent Brief

**Category:** bug

**Summary:** Diagnose the intermittent macOS QUIC v2 failure in `TestHTTP3ServerHotswap`, identify the responsible lifecycle boundary if evidence permits, and propose a separately scoped correction. The maintainer approved local verification and this diagnosis-only brief; implementation is not dispatched by this comment.

**Current behavior:**

The recorded hosted run failed the fresh client's GET `/hello2` with `timeout: no recent network activity` after approximately five seconds. The fixture starts two HTTP/3 servers on one caller-owned QUIC listener, closes the first server, waits for its serving loop to exit, closes the first client transport, and creates a fresh client for the replacement server. `Server.ServeListener` explicitly preserves the caller-owned listener when the HTTP/3 server closes. The failure occurs before response consumption. The timeout text and duration do not establish whether listener acceptance, handshake progress, or request/response handling stopped.

**Desired behavior:**

Produce an evidence-backed diagnosis distinguishing listener acceptance, client/server handshake progress, request-stream handling, response completion, and closure at the hotswap boundary. Establish cause with a bounded reproducer when possible; otherwise document non-reproduction and the smallest missing observation. Preserve caller-owned listener lifetime and upstream API/wire contracts. A passing attempt does not resolve the recorded failure.

**Key interfaces:**

- `TestHTTP3ServerHotswap` — overlapping servers and the transition to a fresh client after the first serving loop exits.
- `http3.Server.ServeListener`, `http3.Server.Close`, and `quic.EarlyListener.Accept` — acceptance cancellation, server-owned connection cleanup, and caller-owned listener lifetime.
- `http3.Transport` and QUIC connection establishment — fresh-client dialing, handshake progress, request delivery, and response completion.
- `quic.Config.HandshakeIdleTimeout` and connection termination — distinguish transport timeout from HTTP application closure using endpoint evidence.

**Acceptance criteria:**

- [ ] Preserve the hosted and local evidence below, including exact source SHA, platform, toolchain, QUIC version, timescale, seed, command, and outcome. Reconcile newer relevant changes before diagnosing current behavior.
- [ ] Establish where progress stopped using evidence from both endpoints where available. Do not infer a handshake failure, listener shutdown, or server lifecycle race from timeout duration alone.
- [ ] Use a finite local investigation: at most one additional seeded suite run with targeted diagnostics and three targeted hypothesis experiments. State each experiment's prediction and changed variable. Stop earlier when cause is established or external evidence is required; report an inconclusive result if the budget is exhausted.
- [ ] If reproduced, preserve the exact failure and minimize it within that budget before proposing a correction, naming the responsible behavior and regression seam. If not reproduced, record the attempts and specify the minimal capture needed from a future natural failure; do not claim a fix or close the unresolved bug.
- [ ] Deliver a diagnosis comment with causal evidence, remaining uncertainty, and a bounded proposed next scope. Remove temporary source instrumentation before finishing. Any subsequent correction needs its own feature worktree/PR, regression evidence, and review after its scope is decided.

**Out of scope:**

- Product or fixture behavior changes, timeout relaxation, test quarantine, CI policy changes, and rerunning an unchanged failed hosted check.
- Broad randomized or stress campaigns, permanent diagnostic infrastructure, and treating a passing retry as evidence of an infrastructure fault.
- Combining #151 or #169 with this issue without concrete evidence of a shared cause, or reopening the separately authorized disposition of PR #187. This investigation does not block that path-probing repair.

## Verification and triage evidence — 2026-09-11

The [full hosted job](https://github.com/the-sarge/quic-go-fast/actions/runs/34549106858/job/103108033710) identifies Go 1.27.1 darwin/arm64, non-race, `TIMESCALE_FACTOR=3`, QUIC v2, and shuffle seed `1789088800293547000`. The checkout is synthetic merge `9820d966cd72fbfe70d55829b971797b05d3ed3f`, whose parents are base `928fc604a1968c8df20379e34fc9778c86ce1102` and PR head `dfd03fc85ead6fa2f33b12cd0a660dea5e5d721a`. The log records the replacement GET failing at 2026-09-11T01:06:51Z; the test lasted 5.07 seconds. The preserved failed-step log matches its original SHA-256 `d592898fbc2936362a2032980a8b21e160443abb28bbd4d552195118f77bad70`. This confirms the historical observation, not its cause.

One local attempt ran the entire QUIC v2 self suite from that exact synthetic merge in a clean dedicated feature worktree on darwin/arm64:

```sh
GOTOOLCHAIN=go1.27.1 TIMESCALE_FACTOR=3 go test -v -timeout 5m -shuffle=1789088800293547000 ./integrationtests/self -version=2
```

The suite passed in 24.850s; `TestHTTP3ServerHotswap` passed in 0.07s. This matches the recorded source/toolchain/version/timescale/seed inputs, but does not reproduce the hosted runner's scheduling, machine load, preceding job steps, or all random inputs. No hosted check was rerun. The worktree remains clean with no source changes.

Local verification output SHA-256: `f6efa08b68208d57a38d078afaae660d7a79b011577e8cf07d7e40ec1ae343c2`. Downloaded full hosted-job output SHA-256: `995b87eea65503d62c44950571281447ab91de1c412906f00c34ed1ec03c3aa3`. Both logs are retained in the local triage evidence directory `/Volumes/worktrees/quic-go-fast/triage-hotswap-188-state`.

Redundancy inspection at main `3336fb1c742fa60475358c20afc0edd20d2edc33` covered the hotswap fixture, HTTP/3 server lifecycle, QUIC listener acceptance, client transport, and subsequent relevant history. Those inspected source files are unchanged between the hosted synthetic merge and this main revision; no established repair was found. No prior-rejection records exist. The applicable compatibility ADR and completed pooled HTTP/3 exchange-lifetime work do not conflict with this diagnosis scope. #151 and #169 remain separate reports with similar symptoms and unestablished causes.
