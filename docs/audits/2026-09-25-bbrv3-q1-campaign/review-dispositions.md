# Q1 bounded review dispositions

Initial review `20260926T050859-bf7ff4c50d7a9b0823175f14`, head `87b8cd5ae9d827b024924e998d78e46872f2090f`.
These are the implementing agent's dispositions after source/contract inspection. The guarantee remains example-level native readiness for the frozen inventory; no controller product behavior changes. Campaign aids retire after Q2. One replacement review remains after verification.

| Cluster | Disposition | Evidence and bounded action |
| --- | --- | --- |
| C-001 | reject | The execution contract requires filtered egress where other traffic shares the pair. Retained reverse capture and receiver both count 17,996,252 probe packets with zero capture drops; aggregate router traffic is not the probe denominator. |
| C-002 | defer | Missing kernel timestamps already increment an invalidating counter. An epoch-sized lag on that invalid record is a diagnostic limitation, not readiness evidence. No router churn or separate ticket for this marginal item. |
| C-003 | fix-now | Inventory metadata names absent Linux facts-3 members and intentionally private Mac files. Bind cloud references to retained archive members/hashes; label private Mac observations external/redacted. No new personal inventory publication. |
| C-004 | fix-now | Generic probe component revision names batch source while portable binary entries name an older revision. Split portable/batch/source-snapshot identities and verify historical hashes. Existing binary identities remain intact. |
| C-005 | reject as duplicate | Same S7 formula/invariant/owner as C-010; no independent semantic root. |
| C-006 | reject | Run.validate admits only reno/bbrv3; the pinned selector implements both, and result controller identity is required by execution validation. New defense-in-depth behavior is not needed for this finite aid. |
| C-007 | reject as duplicate | Same incomplete-FIN completion terminal state and sendSession owner as C-012. |
| C-008 | reject | Timestamp-only scheduling violates the explicit router-first and receiver-readiness procedure. Existing native driver follows that precedence. No accepted execution path proves the claimed fault. |
| C-009 | reject | All 520 expansions were independently enumerated without failure. Publishing another checker is outside the finite evidence obligation. |
| C-010 | fix-now | Scenario multiplies before ceil, contrary to accepted packet-round-then-multiply order. Both S7 directions must have 5,004,880 bytes. One two-direction model regression, one central expression correction, pinned capacity and source, and one affected native S7 recheck. Preserve the old calibration receipt as superseded for S7 capacity. |
| C-011 | defer to Q2 timing | Clock contract requires lease-boundary observations, not repeated per-case clock sessions; the cited Mac driver also includes one-off staging and a 110-second CPU observer. This does not prove recurring 520-case overflow. Q2 must timestamp its first cloud orchestration, distinguish serial per-case work from lease/overlapped work, and reforecast before continuing if the remaining envelope is insufficient. Existing immutable-expiry gate remains mandatory. |
| C-012 | fix-now | sendSession closes an incomplete completion stream at its sender-relative deadline; receiver's admission-relative deadline can be later, so EOF becomes integrity failure. Only complete transfers should emit successful FIN. Leave a deadline-limited incomplete stream open for receiver-owned censoring; preserve actual early errors. Add one real QUIC constrained-transfer regression, preserve verified completion coverage, rebuild fixture identities and run representative native completion checks. |
| C-013 | fix-now | Competitor schedule status predates retained V2 L4/L6-L8 passing preparation. Correct status and evidence pointer only; no Q2 or universal qualification claim. |
| C-014 | reject | Overall Q1/Q2 plan is not fully implemented until Q2 runs; its per-slice table supplies the required prospective post-merge frontier. Header polish is not a behavioral obligation. |
| C-015 | reject | Claimed 490-minute lease assumes eligibility timestamps are actual launch times. Router-first precedence retains the charged case envelope. Same unsupported scheduling assumption as C-008, not a reachable accepted failure. |

## Invariants, owners and terminating evidence

- S7 packet capacity: accepted 100 Mb/s, 100 ms, MTU1460 base queue is rounded first, then multiplied by four. Owner: model.Scenario. Family: both directions, plus existing ordinary/fixed-L3 queue tests for preservation. Test a spec-derived literal, then one native affected-scenario receipt.
- Completion termination: receiver application consumption owns verified versus censored completion; sender cannot emit a successful terminal FIN for a deadline-limited partial payload. Owner: sendSession stream termination; receiveSession keeps premature EOF/corrupt-frame errors. Family: verified target, deadline-limited partial target, actual error; one added constrained real-QUIC regression plus existing successful/integrity tests.
- Inventory/probe/status repairs are process metadata: retained archive member/hash checks and historical component-source checks terminate them.

Contract closure is not triggered: these are campaign-only evidence aids and metadata, with proportionate finite tests rather than shipped safety authority. This is the first Q1 review; no prior accepted review fix exists at these owners. Duplicate reviewer examples identify the same semantic classes, not a repeated-root approach failure.

Native correction budget: at most one 30-minute Linux reservation and one 45-minute Windows reservation, plus representative Mac checks within the already charged eight-hour window. Reserve before launch and retain failures/unused time. Existing 72-hour experiment and $100 ceilings apply; preparation uses the operator's later 72-hour extension. No Q2 comparisons or new platform cross-product.
