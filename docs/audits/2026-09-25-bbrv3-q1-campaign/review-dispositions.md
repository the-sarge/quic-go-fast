# Q1 bounded review dispositions

Initial review `20260926T050859-bf7ff4c50d7a9b0823175f14`, head `87b8cd5ae9d827b024924e998d78e46872f2090f`.
These are the implementing agent's dispositions after source/contract inspection. The guarantee remains example-level native readiness for the frozen inventory; no controller product behavior changes. Campaign aids retire after Q2. The review budget is one initial review and at most one replacement after verification.

| Cluster | Disposition | Evidence and bounded action |
| --- | --- | --- |
| C-001 | reject | The execution contract requires filtered egress where other traffic shares the pair. Retained reverse capture and receiver both count 17,996,252 probe packets with zero capture drops; aggregate router traffic is not the probe denominator. |
| C-002 | defer | Missing kernel timestamps already increment an invalidating counter. An epoch-sized lag on that invalid record is a diagnostic limitation, not readiness evidence. No router churn or separate ticket for this marginal item. |
| C-003 | fix-now | Inventory metadata names absent Linux facts-3 members and intentionally private Mac files. Bind cloud references to retained archive members/hashes; label private Mac observations external/redacted. No new personal inventory publication. |
| C-004 | fix-now | Generic probe component revision names batch source while portable binary entries name an older revision. Split portable/batch/source-snapshot identities and verify historical hashes. Existing binary identities remain intact. |
| C-005 | reject as duplicate | Same S7 formula/invariant/owner as C-010; no independent semantic root. |
| C-006 | reject | Run.validate admits only reno/bbrv3; the pinned selector implements both, and result controller identity is required by execution validation. New defense-in-depth behavior is not needed for this finite aid. |
| C-007 | reject as duplicate | Same incomplete-FIN completion terminal state and sendSession owner as C-012. |
| C-008 | reject | Timestamp-only scheduling violates the explicit router-first and receiver-readiness procedure. Existing native driver follows that precedence. No accepted execution path proves the claimed fault. |
| C-009 | reject | All 520 expansions were independently enumerated without failure. Publishing another checker is outside the finite evidence obligation. |
| C-010 | fix-now | Scenario multiplies before ceil, contrary to accepted packet-round-then-multiply order. Both S7 directions must have 5,004,880 bytes. One two-direction model regression, one central expression correction, pinned capacity and source, and one affected native S7 recheck. Preserve the old calibration receipt as superseded for S7 capacity. |
| C-011 | defer to Q2 timing | Clock contract requires lease-boundary observations, not repeated per-case clock sessions; the cited Mac driver also includes one-off staging and a 110-second CPU observer. This does not prove recurring 520-case overflow. Q2 must timestamp its first cloud orchestration, distinguish serial per-case work from lease/overlapped work, and reforecast before continuing if the remaining envelope is insufficient. Existing immutable-expiry gate remains mandatory. |
| C-012 | fix-now | sendSession closes an incomplete completion stream at its sender-relative deadline; receiver's admission-relative deadline can be later, so EOF becomes integrity failure. Only complete transfers should emit successful FIN. Leave a deadline-limited incomplete stream open for receiver-owned censoring; preserve actual early errors. Add one real QUIC constrained-transfer regression, preserve verified completion coverage, rebuild fixture identities and run representative native completion checks. |
| C-013 | fix-now | Competitor schedule status predates retained V2 L4/L6-L8 passing preparation. Correct status and evidence pointer only; no Q2 or universal qualification claim. |
| C-014 | reject | Overall Q1/Q2 plan is not fully implemented until Q2 runs; its per-slice table supplies the required prospective post-merge frontier. Header polish is not a behavioral obligation. |
| C-015 | reject | Claimed 490-minute lease assumes eligibility timestamps are actual launch times. Router-first precedence retains the charged case envelope. Same unsupported scheduling assumption as C-008, not a reachable accepted failure. |

## Invariants, owners and terminating evidence

- S7 packet capacity: accepted 100 Mb/s, 100 ms, MTU1460 base queue is rounded first, then multiplied by four. Owner: model.Scenario. Family: both directions, plus existing ordinary/fixed-L3 queue tests for preservation. Test a spec-derived literal, then one native affected-scenario receipt.
- Completion termination: receiver application consumption owns verified versus censored completion; sender cannot emit a successful terminal FIN for a deadline-limited partial payload. Owner: sendSession stream termination; receiveSession keeps premature EOF/corrupt-frame errors. Family: verified target, deadline-limited partial target, actual error; one added constrained real-QUIC regression plus existing successful/integrity tests.
- Inventory/probe/status repairs are process metadata: retained archive member/hash checks and historical component-source checks terminate them.

Contract closure is not triggered: these are campaign-only evidence aids and metadata, with proportionate finite tests rather than shipped safety authority. This is the first Q1 review; no prior accepted review fix exists at these owners. Duplicate reviewer examples identify the same semantic classes, not a repeated-root approach failure.

Native correction scope: one 30-minute Linux reservation and representative Mac checks within the already charged eight-hour window. The first 45-minute Windows reservation ended before measurement because endpoint SCP timed out; its full reservation is retained. The replacement 45-minute lease also ended before measurement: bulk stdin transfers stalled, and an alternate download command contained a parameter typo. A third 45-minute Windows lease runs the same unmeasured completion case using a private-network public-binary download with archive/executable hashes and encrypted SSH for the small TLS files. This consumes existing overall contingency without adding a case, repetition or platform. Reserve before launch and retain failures/unused time. Existing 72-hour experiment and $100 ceilings apply; preparation uses the operator's later 72-hour extension. No Q2 comparisons or new platform cross-product.

## Implemented correction evidence

Source commit `e4f322cbbfd4225a4b714e08ec19c958cccadcb0` contains the two central behavior fixes and their regressions. [Local validation](evidence/review-local-validation.json) records red/green results, affected-package race tests, vet, tidy, inventory/probe-source checks, and all 520 command expansions. [Mac correction receipt](evidence/mac-review-correction-summary.json) records the rebuilt native fixture's verified 16 MiB completion and unchanged gateway gates. The outer wrapper's redundant cleanup failure is retained with independent absence verification and exact lease release; it does not rewrite the successful native role outcomes.

The infrastructure guard's first full certification encountered a local Go bootstrap mismatch in a deliberately offline guest test. Invoking the installed Go1.27.1 binary directly made that focused test pass; the complete unchanged-head `task check` subsequently passed with that environment at `3ddfc5a6590a65ef85c03989875534e8751dacab`. Hosted run [36224328143](https://github.com/the-sarge/infra/actions/runs/36224328143) passed on the same head. Infra PR #643 merged as `8f46b44689014acf5351bdf695ac7e76139d6d4c`, and that source was staged on the retained Controller before the correction leases. No source fix or RAS timeout change was made for it.


[Windows staging failure](evidence/windows-review-staging-failure-summary.json) preserves the timeout and successful cleanup. Ordinary SSH worked and the other endpoint received the same bundle; the exact SCP stall cause is not claimed. The bounded workaround changes transfer transport only, with end-to-end archive and executable hashes.

[Second Windows staging failure](evidence/windows-review-retry-failure-summary.json) preserves the failed stdin experiments, the incorrect download parameter and cleanup/resumption outcomes. The corrected download command changes staging only; all reservations are retained and no native completion measurement was repeated.


[Linux correction evidence](evidence/linux-review-correction-summary.json) confirms the corrected 5,004,880-byte saturated S7 queue, fresh four-core gateway facts and verified 16 MiB completion. [Windows correction evidence](evidence/windows-review-correction-summary.json) verifies completion with the rebuilt fixture. Both include before/after clock observations, unchanged gateway gates, successful explicit teardown and independent empty VM/disk inventories. These complete the accepted native correction checks; source identities and old failed observations are preserved. Verification and replacement-review judgments are recorded with the live PR rather than changing the frozen measurement receipts.

## Initial-review verification disposition

Verification `20260926T050859-bf7ff4c50d7a9b0823175f14-verification-1790410204693551000` examined head `6b8b41e7f8b8fd42a9a98d446e9e8eb50682644f` and covered all 15 original clusters. All accepted fix-now roots resolved. C-002 and C-011 remain open in the verification output with their existing independent defer dispositions above; no new evidence changes those judgments. The blocking projection is clear; security was not examined by this verification.

New compiler-attribution finding `sha256:v2:dc5a522f812876080f7ea3c9ac1f3b11ce289246324f505643aaf3ff049d03bd`: **fix-now**, process metadata. Embedded `go version -m` information and native `runtime.Version()` receipts agree that the corrected Linux, Windows and Mac fixtures were built with Go1.27.0; the graph incorrectly transcribed Go1.27.1. The corrected router genuinely uses Go1.27.1. Correct those three fixture fields and the review-build prose, retaining every binary and frozen native record. The finite evidence is comparison with all four rebuilt executables and their recorded SHA-256 identities.

Precise-root comparison with C-004: C-004's invariant is that portable and batch probe source revisions have distinct, truthful component identities; its owner is the probe source mapping, and its semantic classes are historical portable versus batch probes. This finding's invariant is truthful compiler attribution for the rebuilt fixture family; its owner is build metadata transcription, and its classes are the three rebuilt fixture executables versus the native router. There is no shared central enforcement seam, and splitting historical probe source identities was not required to correct later fixture compiler transcription. The shared graph file does not make these the same precise root. Contract closure remains not triggered for this bounded traceability correction.

This cheap, high-confidence documentation correction uses the shared docs-only policy: no additional verification cycle or native run. The one allowed replacement review receives this disposition and the original verification receipt; final exact-head certification and hosted checks still apply.
