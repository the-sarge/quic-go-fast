# I3 empty-input preservation re-audit

The I3 replacement review `20260910T075829-87baedff1ad560d986c4de7a` identified a supported producer state omitted by preflight. At candidate `8887f2e746e55af5e67fea3d8d4b3a24516cc4e7`, `oobConn.ReadPacket` returns `receivedPacket{}, nil` after a zero-message nil-error batch read, and `Transport.listen` forwards it. The new unconditional empty-input release would panic. Findings `CLAUDEFABLE-F-001` and `CLAUDEOPUS-F-001` are duplicate reports of that one case; this is unrelated to the initial formatter finding and does not establish a repeated precise runtime root.

Disposition: stop for scoped re-audit, then fix-now after publication and child-pointer synchronization. The accepted preservation obligation is to consume owned empty input without changing the existing bufferless sentinel no-op. The concrete enforcement owner remains the empty-input branch of `Transport.handlePacket`. Its finite semantic matrix is two subcases of the existing empty-input row: non-nil exclusive buffer → release once; nil buffer and empty data → return without buffer access. Nonempty bufferless input is unsupported. Existing parsers still own wire syntax; no new representation choice is made. I7 retains zero-progress reader retry and cache bookkeeping.

Runtime disposal and the nil guard are shipped behavior and required safety enforcement. The single sentinel regression is a verification aid, not a maintained observer framework; the existing disposable pool probe remains disposable and applies only to acquired buffers. Plan and receipts are traceability metadata. Closure remains not triggered for these finite guarded transitions. The evidence budget remains the ten named cells, with owned and bufferless subcases in the empty-input row, ordinary preservation tests, one focused race selection, unmodified local certification, and inherited hosted gates. No mutation, timing deadline, stress count, extra platform or universal parser claim is added.

The producer census traces basic-reader owned empty datagrams and OOB bufferless no-progress sentinels through listen to the same transport guard. There is no persisted authority, restart schema, second terminal owner, duplicate representation, new goroutine or temporary product seam. No newly material effect remains untraced. Splitting the nil guard into I7 would leave I3 introducing a process crash; merging I7 would import a different cache/retry lifetime and exceed this slice. The central guard and one regression fit the existing I3 context and one independently green product PR.

The process-wide panic is a directly in-scope critical safety defect despite the reviewers' medium labels. Record the shared review-budget exception: after publishing this clarification, permit the central correction, verification of the triggering replacement at the corrected exact head, and one final fully briefed fresh review. No other review or evidence expansion is authorized. The product branch is preserved in PR #160. The normative contract contains the current rule and evidence ceiling; review chronology and receipts remain here and in the [product PR](https://github.com/the-sarge/quic-go-fast/pull/160).
