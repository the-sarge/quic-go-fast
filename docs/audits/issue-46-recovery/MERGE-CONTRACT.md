# Issue #46 test and evidence publication contract

The maintainer invoked `loop-review-merge` after asking whether the permanent tests and two investigation reports needed merging. This contract governs publication of commits `16423f27` and `d3fd3369` on current main. The clean feature branch starts at `d678ff1842e8439c87a0556e65080fdf4e1a241d`; cherry-picks preserve the original audit artifacts and test bytes. Earlier diagnostic budgets, no-CI restrictions and no-merge statements describe those completed phases. The current merge instruction authorizes the finite review and ordinary local/hosted certification below; it does not authorize a new random investigation or a transport redesign.

## Accepted outcome and criteria

The approved follow-up sequence in [PLAN.md](PLAN.md) remains normative:

> Public `Transport.Listen`/`Transport.Dial`, a simulated `net.PacketConn` boundary, and a bidirectional stream exchange are the seams accepted by the maintainer's “go”. Use real TLS, packet processing and recovery with `testing/synctest`, a fixed 5 ms RTT and an unchanged one-second Dial budget. A 4096-byte exchange bounds the new fixture's transfer work; the original 500 KiB MITM assertions are not edited. No forced internal recovery state, mocks, shuffled-test seeds or replayed cryptographic bytes.

> C1: replace the final protected byte of the first server Initial packet with its XOR-1 value; forward all later traffic unmodified. Expect one authentication rejection and successful handshake/transfer through Initial recovery. C2: same selection, no byte mutation. Expect successful handshake/transfer without that authentication failure. Each is one focused execution, no race, macOS Go 1.27.0.

> C3: replace the final protected byte in each of the first three server Handshake-bearing datagrams, locating the target QUIC packet within any coalesced datagram. Then deliver all traffic unchanged. Expect consecutive authentication failures followed by a usable Handshake recovery probe and completion before deadline. C4: same selection rule without byte mutation. Expect completion; successful TLS progress may mean fewer selected Handshake datagrams than in C3. Each is one focused execution in the same environment.

> C5 (predeclared, focused usage 7/8): corrupt every server Handshake packet selected by the same protected-byte rule until the one-second Dial context expires. Use a practically unbounded selection limit (`math.MaxInt32`) so the timer, not a count cutoff, ends the case. Expect repeated authentication failures and continued PTO/probe activity, `context.DeadlineExceeded`, no returned connection, and all transport/network goroutines shut down by fixture teardown. This validates cancellation when the network supplies no usable Handshake data; it is not evidence of a recovery defect. No successful transfer is expected in this separate failure-outcome test.

The permanent `TestHandshakeCorruption` matrix discharges those bounded behavior cases. Deliver it plus the immutable investigation traces/reports; leave #46 open because historical causation is unresolved. Preserve production, original MITM corruption decisions, random draw order, payload, deadlines, upstream API/wire compatibility and packet-emission ownership. The approved diagnostic extension below permits observational edits to the original corruption test. Merge one focused PR; append the development journal only after the main PR is merged.

## Representation, artifacts and ceilings

The guarantee is **example-level regression coverage**, not universal recovery correctness, all corruption schedules, arbitrary packetization, all QUIC/TLS implementations or historical replay. Supported input is the repository's actual QUIC packet output in this simulated endpoint fixture. Existing `wire.ParsePacket` owns packet representation; the selector locates a protected byte inside the parsed target packet, including behind a coalesced Initial. Existing `simnet` and `testing/synctest` own network copying/delivery and the controlled clock. The active test owns the selected fixture scenarios and public assertions; it must be retained with them. Changes to future transport defaults may require updating a fixture rather than changing the protocol to satisfy one-second timing.

The new active Go test is a maintained **verification aid**, with the operational payoff of reproducible bounded-corruption recovery and cancellation coverage. It is maintained alongside integration tests; retire or revise it only when the supported fixture/public behavior changes. Raw patches, historical logs, captured JSONL and executable one-off analyzers are **process/traceability metadata and archival verification aids**, not runtime code or mandatory product validation tooling. Their artifacts preserve historical commands, heads and failure evidence. Merge/PR/journal receipts are process metadata. No shipped runtime behavior or safety enforcement is added.

Do not broaden this PR into proxy redesign, random-test policy changes, transport fixes, decoder conformance, mutation campaigns, fixture cross-product enumeration, workflow standardization, or a new architecture program. Contract closure is not triggered: these example-level verification aids do not introduce a material-risk universal enforcement obligation. Adjacent or stronger guarantees are independently dispositioned as defer/reject; a real in-scope unsafe gap requires stop-for-decision rather than silently widening the task.

## Terminating review and certification plan

One fully briefed initial RAS review; at most one fresh replacement review after independently accepted fixes. Use two configured independent reviewers (`codex-astra`, `codex-sol`) with normal adjudication and synthesis, a 900-second per-agent bound, and tiered context appropriate to a 122-line active test plus archived evidence. At least two successful reviewers and a usable synthesis are required. No automated fixer. RAS verifier runs are scoped to accepted findings and pinned to the pushed head. Prior Standards/Spec reviews are historical evidence, not substitutes for this RAS review.

No randomized capture campaign or mutation run is planned. Accepted code fixes may consume one targeted validation per affected case plus one final certification per changed candidate. For the final clean pushed head, run the five cases once with race detection, one native full `go test -count=1 ./...`, `go vet ./...`, repository formatting/lint, and non-mutating module-tidy checks. Full-suite coverage here is ordinary merge certification, not a reset of any historical investigation budget. Hosted CI covers the repository's declared operating-system/toolchain matrix and generators/cross-compilation. Do not rerun a failed same-head check until green; first diagnose it. A proven infrastructure failure may justify a same-head rerun; an in-scope code defect returns to disposition/review/certification. Main advancement requires rechecking the base and applicable gates.

This repository has no `task preflight`, separate review protocol, portfolio `ci.yml`, draft gate, required `ci-*` names or branch-required-check rules. Its upstream-style `unit.yml`, `integration.yml`, `lint.yml`, `cross-compile.yml` and other triggered workflows run on push/pull_request even while draft. Use unskipped completed PR checks on the exact live head as the repository's hosted equivalent, require relevant checks to pass, and record their run IDs. Mark ready only after local certification and review; inspect live PR checks/head before squash merge using `--match-head-commit`. Do not add or rerun workflows merely to manufacture nonexistent portfolio job names or a ready-for-review event this repository does not configure.

## Approved failure-only diagnostic extension

The maintainer explicitly requested: “extend this PR with bounded, failure-only diagnostics in the original corruption test, preserving its randomness and deadlines”. This supersedes the earlier restriction on editing the original MITM test for observation. The prior RAS review at `bc7ed2e7` found zero issues; a fresh initial review of the expanded outcome and at most one replacement after accepted fixes are authorized by this new implementation/review cycle.

Capture the original `TestMITCorruptPackets` scenario (direction, version, RTT and effective Dial timeout), latest client/server Dial/Accept/stream-transfer phases, a bounded tail of existing connection/endpoint qlog events, and proxy decisions with datagram checksums, selected byte offset and before/after value, replacement-write byte count and error. Reuse the existing 256-event, 4096-byte-per-record collector and authoritative qlog encoder. Emit a clearly labelled corruption diagnostic report through ordinary test-failure cleanup, after transport cleanup. Passing cases emit no diagnostic report; explicit user-requested `-qlog` output retains its existing behavior. Retain scenario/latest phases independently of event eviction and disclose truncation/omission.

The maintained aid belongs to the original corruption fixture. Its operational payoff is useful evidence from the next naturally occurring failure; maintain or retire it alongside that fixture. Preserve the exact per-callback random draw sequence and bounds, including the existing `IntN(4)` long-header selection, the possibility of choosing the original byte, one immediate replacement WriteTo, original drop decisions even on write error, 500 KiB exchange, and existing scaled context/timeouts. Observation adds scheduling overhead and does not claim timing neutrality. A callback forwarding decision is not socket delivery; a successful write is not peer receipt. No causal claim, universal recovery proof, full trace/replay, process-kill/timeout capture, production change, or CI exception is included.

Accepted verification seams are the proxy callback's supplied decisions/random draws and replacement writer, the existing bounded diagnostic report/qlog interface, and actual corruption-fixture cleanup. Deterministic tests cover callback draw/write transparency for both directions, selected/no-op mutation and write errors, and passing/failing real fixture cleanup. Existing collector tests discharge storage bounds, truncation, snapshot ownership and concurrent producer obligations; do not recursively expand them. Use one targeted red/green execution per new behavioral test, one final focused race check, one uncached native full suite, relevant static checks, and ordinary hosted certification at the final reviewed SHA. No retries-until-green or new random campaign. The captured hosted failure at `bc7ed2e7` remains unresolved historical evidence even if the new head passes.
