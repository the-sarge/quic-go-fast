# First-milestone handoff audit — 2026-09-06

This is historical audit evidence for [QGF-2026-09](../adr/2026-09-06-fork-program.md). The current track plans own the contracts. No transport implementation, runtime regression, or benchmark was executed during this handoff.

## Scope and source

The audit compared the proposed D1, D2 and H1 slices with upstream v0.62.0, `793f74d8e03368c5aded128af6f48d21dbb47f73`. An independent read-only agent inspected an immutable draft snapshot and pinned source; the author independently dispositioned its findings. The revised clauses received scoped verification. No existing fork implementation or unresolved fork PR was treated as a baseline.

## Slice decisions

| Slice | Owner and end-to-end outcome | Audit conclusion |
| --- | --- | --- |
| D1 | Receive queue admission owns whether an incoming frame merits an owning allocation | One complete queue change, independently green; bounded tests and paired measurements. Splitting characterization into a predecessor supplies no independent product outcome. |
| D2 | The same receive owner adopts existing lazy ring mechanics without changing shared ring code or payload ownership | Independent of D1. Keeping them separate attributes cost changes; serialize integration because their file overlap is real. Source inspection supports reference clearing; inherited ring tests cover FIFO/wrap behavior. |
| H1 | The connection loop owns fallback, the send worker publishes bounded immutable feedback, and the existing finder owns confirmed-path probing | One complete recovery behavior across both endpoint constructors and send/confirmation paths. Splitting plumbing or endpoint roles would leave unused machinery or incomplete shared invariants. The declared context/evidence budget and stop conditions bound implementation. |

No genuine blocking edge exists among these slices. H1 can proceed beside either receive slice. No persistence authority or temporary duplicate state machine is introduced. Shipped changes, existing/focused verification aids and process receipts are classified in each plan; there is no separately maintained verification-aid deliverable. D1/D2 use ordinary focused queue evidence; H1 has a finite lifecycle/concurrency matrix. No untraced effect is accepted as permission to widen a slice.

## Finding dispositions and verification

| Finding | Evidence and obligation | Disposition and resolution |
| --- | --- | --- |
| H1 proposed peer-bounded initialization also changes the healthy path | `connection.go:2443` passes the configured start directly; `mtu_discoverer.go:116` does not clamp it. The contract promises healthy sizing preservation. | **fix-now** — preserve baseline healthy initialization and existing discovery upper-bound calculation. Only the error-driven effective start changes to 1200, allowed by `internal/wire/transport_parameters.go:323`. Decision and acceptance clauses corrected and verified. |
| D2 overstated inherited test coverage for clearing references | Existing `ringbuffer_test.go` checks length/FIFO/wrap, not pointer-slot clearing. The unchanged `PopFront` explicitly clears the backing slot. | **fix-now** — accurately require source-owner inspection plus inherited FIFO/wrap tests. No GC timing test, shared ring edit or new harness obligation. Corrected clause verified. |

These are different precise invariant/owner roots: healthy connection initialization versus evidence attribution for unchanged ring storage. They do not establish a repeated-root approach failure. There are no unresolved blocking findings or deferred work items from this audit.

H1's congestion floor was traced through initial construction, absence of pre-confirmation probes, later monotone growth and both migration seams. `internal/ackhandler/sent_packet_handler.go:1120` resets congestion accounting to the configured initial packet size on migration. Lowering the setter directly and using finder `Reset` before confirmation remain explicitly rejected. Actual recovery remains a hypothesis to reproduce before runtime editing; audit acceptance is not a claim that the regression or physical Tailscale qualification has passed.

## Publication evidence boundary

The documentation gate is one independent audit with scoped finding verification, Markdown/link inspection and whitespace checks on the published head. The authored files use one physical line per prose paragraph. Branch protection and rulesets were inspected before publication: no hosted check was required on `main`; inherited workflow availability is separate from runtime certification. The docs PR owns the exact-head validation and merge receipt. Child issue plan commits must be resolved from the merged default branch and checked for reachability before reporting readiness.
