# Architecture handoff slice audit — B1, H1, C1, Q1, M1

Read-only source audit pinned to `fdb1af2a2c81e441ad3c855c355e136214e3fd77`. Drafts read from `/Volumes/worktrees/quic-go-fast/architecture-handoff-20260913/docs/adr/2026-09-13-*-plan.md`. Read architecture-handoff and the shared REVIEW-LOOP and CONTRACT-CLOSURE baselines. No implementation, tests, repository edits, branch changes or external writes performed. This file is audit evidence, not a replacement normative contract.

## Result

The five one-PR boundaries are coherent and independently green. All five genuinely have no slice blockers. Approve their graph after the source/owner/evidence corrections below and publication of the linked existing-work audit. None requires a new design decision, another slice, or an implementation predecessor.

## Required draft corrections

1. **C1 source filename:** `sys_conn_oob_windows.go` does not exist at the pinned source. Use `sys_conn_windows.go:159–227`.
2. **C1 representation owner:** Nonpositive segment-size normalization currently belongs to Unix reader `sys_conn_oob.go:271–275` and Windows reader `sys_conn_windows.go:203–206`. The new holder absorbs that normalization; `coalescedSlab.split` owns positive-size splitting and explicitly panics for nonpositive input at `coalesced_slab.go:68–70`. Do not state that the splitter already owns the nonpositive-size semantics.
3. **C1 actual Windows evidence selector:** `Coalesced|GRO|ReceiveOffload` misses `TestWindowsUROReadEmptyDatagram`, `TestWindowsUROReadPacketInfoInheritance`, and `TestWindowsUROReleaseReadBuffersReleasesPendingViews`. Give those concrete names or use a bounded `^TestWindowsURO(Read|ReleaseReadBuffers)` selector in the Windows evidence command, plus the shared slab tests as appropriate. The Linux relevant tests are `TestGROReadSplitsCoalescedRead`, `TestGROReadEmptyDatagram`, `TestGROReadPacketInfoInheritance`, and `TestGROReleaseReadBuffersReleasesPendingViews`. Native Windows execution remains needed for the actual wrapper; compilation is insufficient.
4. **Q1 owner and context:** The real receiver/queue owner is `stateTrackingStream`, not `datagramStream` (`http3/state_tracking_stream.go:142–176`). Update the single-owner and dispatch-context descriptions. Tighten the source range to `:156–177` when discussing ReceiveDatagram.
5. **H1 source filename:** Qualify `response_writer.go` as `http3/response_writer.go` so the source anchor resolves from repository root.
6. **B1 closure trigger:** The draft asserts closure is triggered but does not establish the shared policy's second condition that ordinary focused tests cannot reasonably cover the independently reachable cases. Four numeric-boundary/error cases at one branch are directly covered by ordinary focused tests. Mark closure **not triggered** and retain the compact table as an ordinary accepted behavior/evidence table, without imposing a stronger closure obligation. Fatal no-resend and release remain required acceptance behavior.
7. **M1 artifact classification and deletion scope:** This slice removes a verification aid and its generation intent; it does not change runtime behavior or introduce safety guards. Replace the generic runtime/guard classification accordingly. Explicitly remove the generation-only `type PacketHandler = packetHandler` alias at `mockgen.go:39` with the directive and output. Pinned Go-source census finds the alias referenced only by its directive; the real lowercase `packetHandler` remains untouched. This is already the coherent generator-removal outcome, not additional production scope.
8. **Existing work publication:** At audit time the linked `docs/audits/2026-09-13-architecture-handoff/existing-work.md` did not exist. The parent agent is assembling it. The audit may pass only once this document supplies the claimed relevant open-work dispositions; a missing link is not evidence of no open work.

## B1 — Fail closed on impossible send-batch progress

**Delivery and owner:** The actual sendQueue worker interprets `batchSender` results, aborts impossible progress before fallback, preserves a supplied error, releases the full offered group through the existing defer, and leaves valid partial progress behavior intact. `send_queue.go:218–268` supports the claim. The Darwin adapter already has its own defensive capability policy; no new worker latch is needed. The adapter count/error contract is the trusted finite numeric representation. Universal no-resend on invalid results is enforceable centrally at this branch; tests are representative enforcement evidence, not network-duplication proof.

**Boundary and blast radius:** One result branch plus focused tests; no public interface, wire layout, persisted authority or platform callback changes. Sender availability signaling and Close's queued-buffer drain stay within traced preservation scope. The invalid count must be checked against the currently offered remainder, not original group length. Zero count with nil error is valid and must still fall back. No duplicate state or temporary adapter is introduced, and no successor is required for correctness.

**Evidence/context:** Four invalid-count cases covering negative/over-offered with nil/non-nil errors, plus existing valid-progress and release/drain regressions, fit one fresh context containing the worker, its interface contract, the existing fake, and the Darwin guard paragraph. No mutation, platform campaign, or new harness is needed. Frozen diagnostic source/logs remain evidence, not a maintained blocking tool.

**Split/merge decision:** Splitting error generation from no-resend could merge an unsafe intermediate result and is not coherent. The strongest merger is another send-worker cleanup because of shared files, but it has no necessary shared invariant and would widen accepted scope. No blockers are necessary; ordering this after receive lifetime would be purely scheduling convenience. Existing work disposition must be backed by the pending audit.

## H1 — Finish normal responses inside responseWriter

**Delivery and owner:** The private method owns existing Content-Length inference, Flush and trailer attempt; RawServerConn retains panic/hijack handling, CancelRead and stream Close. `http3/server_conn.go:247–269` establishes the exact preserved order. `http3/response_writer.go:265–290` confirms that flush and trailer errors log independently. The header predicate must remain `headerWritten`, and map key presence must remain distinct from empty/nil contents. One method call is a real improvement in private-field locality even with one caller.

**Boundary and blast radius:** A synchronous extraction through one caller; no new public API, lifetime owner, error propagation, persisted state, concurrency primitive or allocation representation. No temporary seam exists. The server early-return paths must still bypass finalization. Tests that invoke decodeHeaders while continuing to write must keep their present helper semantics.

**Evidence/context:** Eight bounded characterization rows using current HTTP parsing/QPACK owners, one package run and code-level two-operation order fit a fresh context of the completion block, writer and relevant tests. Closure is correctly not triggered. Explicitly distinguish representative HTTP states from universally enforced local Flush-then-trailer sequencing. No stronger grammar guarantee is needed.

**Split/merge decision:** Separating inference from flushing retains caller coordination and adds a needless intermediate method; splitting tests from the caller extraction is not independently demonstrated behavior. Merging HTTP/3 admission adds unrelated concurrency and state. No genuine blocker exists; sharing a package is not a dependency. Existing work disposition is conditional on the pending audit.

## C1 — Share coalesced receive pending-view lifetime

**Delivery and owner:** A private concrete per-reader holder consumes already-filled nonempty backing, normalizes the reader's segment-size convention, relies on the existing splitter to establish all sibling references before first publication, returns ordered metadata-preserving views, and disposes only unreturned references. Holder state is reader-confined; the existing slab owns atomic backing lifetime. Unix preposted native buffers remain owned by `oobConn.releaseReadBuffers` after the shared holder discard. Both current implementations support this boundary.

**Boundary and blast radius:** This is the widest of the five slices but remains three small methods and two wrappers, with finite metadata and release-order preservation. Existing splitting and atomic slab lifetime are not redesigned. Empty and nonoffload input bypass the holder. No extra backing allocation, generic receive engine, ring, cursor, syscall, capability or retention-policy change is included. Closure is justified here by multiple independently reachable wrapper/cleanup paths and concurrent consumers of returned siblings; its semantic matrix is bounded to this lifetime.

**Evidence/context:** Shared singleton/multiple/short-tail, metadata/FIFO, release-before/after-discard, repeated discard and concurrency fixtures plus actual platform bypass/native-cleanup evidence are sufficient. Clarify that the eight-case cap concerns the bounded shared characterization additions/reuse; existing named platform regressions are explicitly budgeted separately, avoiding a misleading implication that numerous inherited tests count as eight total subtests. One Linux race run, one Windows wrapper run and Darwin Unix integration are justified semantic platform distinctions, not confidence repetition. Current slab source/tests, two wrappers and only the relevant completed I7/W3 invariant paragraphs fit one context.

**Split/merge decision:** A strongest split is introduce holder and migrate Unix, then migrate Windows. It can be green but deliberately retains duplicated lifetime logic, adds transitional bookkeeping and a second PR without reducing an already small context. Keeping both wrapper migrations together delivers the consolidation and removes the seam in one step. Moving only pending pop or only splitting does not close the repeated adoption/discard ownership. No blocker exists because the underlying slab and offload implementations are merged; completed I7/W3 are reference invariants, not new tasks. No untraced effect remains inside the stated boundary after source and selector corrections.

## Q1 — Clear consumed HTTP/3 datagram queue slots

**Delivery and owner:** Under the existing `stateTrackingStream.mx`, copy the returned data reference, clear queue index zero, then advance the slice. The caller keeps returned data. Existing queue-before-recvErr ordering at `http3/state_tracking_stream.go:159–167` is preserved, so terminal drain must not be added.

**Boundary and blast radius:** One release of retained reachability, bounded queue length, no claimed GC timing or allocation reduction. No additional lock, copy, queue representation, public contract or persisted owner. Closure is correctly not triggered; the universal single-pop slot-clearing rule is mechanically enforced at one canonical index.

**Evidence/context:** Existing `TestDatagramReceiving` covers blocking, bounded FIFO and context cancellation. It does not currently test queued-data-before-recvErr, which can be preserved through source reasoning for this one-assignment edit; a narrowly combined retained-reference/queued-drain regression is acceptable within the one-new-test cap if needed. Do not require a GC-timing fixture or a test that merely mirrors the assignment. One method and its surrounding receive/close operations fit easily.

**Split/merge decision:** One reference-clear operation cannot be split meaningfully. The best merger is C1 because both clear consumed slots, but their data types, owners, platform risk and evidence differ; no correctness dependency exists. Keep independent. No temporary seams or blockers exist, conditional on the pending existing-work audit.

## M1 — Remove the unused MockPacketHandler generator output

**Delivery and owner:** Remove generated test output plus the directive and now-unneeded generation alias, preserving the real runtime interface. Current-source grep shows all MockPacketHandler occurrences in its generated file and the PacketHandler alias only in mockgen.go. The generator remains the sole authority for maintained output; frozen historical evidence remains byte-stable.

**Boundary and blast radius:** Test build and generation reproducibility only; no runtime state or authority transition. Generated mock code is a verification aid, the directive/alias is generation input for that aid, and plan/audit/mirrors are traceability metadata. This work removes an unused aid and does not promote generation infrastructure into a new maintained blocking product deliverable. No transitional seam remains.

**Evidence/context:** A Go-source/reference census including the alias, regeneration, root package tests and an expected-diff check are finite. The existing `.github/workflows/go-generate.sh` deletes and regenerates tracked generated files, then checks `git diff --exit-code`; its dirty-worktree failure must not be misdiagnosed as stale generation when the intended deletions are uncommitted. Either use `go generate ./...` and compare the expected diff during implementation or run the script at the clean candidate after committing. Unexpected regeneration churn is not permission to rewrite unrelated outputs or frozen evidence.

**Split/merge decision:** Directive/output deletion must be one patch or regeneration restores the mock. The alias is part of that same obsolete input. The strongest merger is another trivial cleanup, but unrelated production cleanup would add an owner and evidence scope without making either slice more coherent. No blocker exists, and the deletion context is very small. If a new maintained consumer appears, re-audit rather than deleting its tests.

## Cross-slice conclusion

There are no genuine hard edges among these five or from them to the reviewed receive/admission tracks. File overlap during concurrent implementation may warrant ordinary rebase coordination, never a false graph dependency. Each artifact class, no-persisted-authority result, lack of temporary seams and narrow context claim holds after the named corrections. No additional performance experiment, recursive verification obligation, architecture expansion or user technical decision is warranted.
