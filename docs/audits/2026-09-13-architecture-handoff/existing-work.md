# Existing-work and publication-path audit

Source inspected read-only: `fdb1af2a2c81e441ad3c855c355e136214e3fd77` in the initiating checkout. GitHub issue/PR state was read on 2026-09-13. Read `_shared/CONTRACT-CLOSURE.md` before classifying roots. No repository edits, implementation, branch movement, CI reruns, or external writes were performed.

## Disposition

None of the twelve open issues or two open PRs supplies an unmerged prerequisite for the nine accepted improvements. Build from merged default-branch source. Preserve independently open investigations; do not claim these repairs solve the recorded intermittent failures. Do not reopen completed I/G/W/D programs.

### Open PR #131 — stale alternative, not a dependency

[PR #131](https://github.com/the-sarge/quic-go-fast/pull/131), head `6c4b1798492421f18a17c49070ed1801ae5f7487`, branch `codex/timeout-worker-teardown`, remains OPEN with no comments or submitted reviews. Its body records an incomplete RAS cycle and a macOS Go 1.27 QUIC-v1 nobody-speaks packet-loss timeout at `handshake_drop_test.go:139`; the same-head push run passed independently, which does not erase the failure. The historical stop remains its own receipt.

The full diff changes timeout wrappers, cancellation/deadline fixtures, `receive_stream_test.go` and `send_stream_test.go`, plus proposed `testutils.RunWithTimeout`. It does not change runtime `receive_stream.go`, stream frame disposal, batch progress, HTTP/3 server admission, response completion, coalesced delivery, or HTTP/3 DATAGRAM storage. Its Read/Peek helper overlap is a test-file integration concern only.

Crucially, [PR #134](https://github.com/the-sarge/quic-go-fast/pull/134) independently delivered issue #70 at merged commit `7607293b93da7c1fe3ae505454868630fb987efd`, reachable from the pinned baseline, and journal #135 records completion. Its reviewed head was `b52da1761eb6603be989cd7a4fa0b1e7f7844aed`; body records full local certification, initial RAS, exact-head verification, clean replacement, and all 33 hosted checks. The implementation is not byte-identical to #131: current main uses local wrappers rather than #131's new shared `testutils/timeout.go`; cancellation accounting/error draining also differs. Reuse current merged helpers, never cherry-pick #131 as a prerequisite or declare its failed receipt certified by #134. Administrative closure of the stale PR is outside this handoff.

### Open PR #124 — old journal only

[PR #124](https://github.com/the-sarge/quic-go-fast/pull/124), head `c6a036c3d3fe1cd5c2a0f1176bdf14e17b05d088`, branch `codex/gso-capability-65-journal`, changes only a 13-line append to `docs/DEV-JOURNAL.md` for already merged product #122 (`5e9ae2ab3e46067335ea409a689b638a82877bd3`). Its comment preserves an unresolved journal-certification packet-loss timeout at `handshake_drop_test.go:139` (Ubuntu Go 1.26). It has no product overlap and supplies no baseline code. Leave its history and stale append untouched; do not graft it into new handoff docs or journal work.

### Open issue census

| Issue | Actual overlap and disposition |
| --- | --- |
| [#188](https://github.com/the-sarge/quic-go-fast/issues/188) HTTP/3 server hotswap timeout | Shares `http3.Server.ServeListener`/`Close` and caller-owned listener behavior with server admission work. Historical replacement GET `/hello2` timeout has no established causal owner; full exact-source seeded local suite passed. Existing diagnosis brief permits only a bounded investigation, no implementation. Preserve caller-owned listener lifetime and include current hotswap behavior in relevant regression selection. Do not claim admission race explains this timeout or make #188 a blocker. |
| [#151](https://github.com/the-sarge/quic-go-fast/issues/151) HTTP/3 initial request with short idle timer | Shares server construction/request lifecycle, not an established admission-counter fault. Latest diagnosis exhausted its one-suite/three-experiment allowance without reproducing the historical error. Forced experiments demonstrated reachable early idle closure and a distinct custom Dial-channel retry stall, neither identified as historical cause. No new experiment or timer-start policy is authorized by the handoff. Preserve idle-timer behavior; keep the unresolved issue separate. |
| [#169](https://github.com/the-sarge/quic-go-fast/issues/169) HTTP/0.9 initial-request timeout | Distinct protocol fixture. Cleanup/diagnostics from #168 already merged; timeout cause unknown. Waiting on one natural failure capture, zero new test invocations allocated by triage. No dependency on HTTP/3 response finish or admission. |
| [#241](https://github.com/the-sarge/quic-go-fast/issues/241) TestDial/DialAddr timeout | Root transport/socket/handshake investigation. Seeded package and focused attempts passed; no established repair. None of nine accepted changes has a demonstrated causal relationship. No inherited diagnosis budget. |
| [#178](https://github.com/the-sarge/quic-go-fast/issues/178) MTU convergence | Existing convergence assertion and DATAGRAM limits, not queue slot lifetime or batch progress. Includes a later W2 certification observation. Preserve established MTU/offload contracts; no causal common-root claim or reopening through this program. |
| [#149](https://github.com/the-sarge/quic-go-fast/issues/149) atomic receive-batch capture | Accepted standalone diagnostic enhancement: `corruptionCapture` serializes a returned batch's result/members against recorder finalization, bounded by existing storage limits. This is a verification-aid operation boundary; coalesced delivery owns runtime sibling packet publication/storage. Do not merge these contracts, depend on #149, or copy its recording completeness guarantee into runtime code. |
| [#44](https://github.com/the-sarge/quic-go-fast/issues/44) packet-loss timeout | Maintained diagnostics from #132 are merged. Latest triage waits for one natural capture with zero new invocations; prior randomized allowances are exhausted. The server-first wait and nobody-speaks dial cases have no established shared causal owner. Preserve old receipts; no new campaign, no implicit CI waiver. |
| [#111](https://github.com/the-sarge/quic-go-fast/issues/111) foundational architecture map | Deliberately separate paper/source research about retaining the incumbent versus an explicit engine. This handoff is bounded improvement to the incumbent, neither completion nor rejection of that inquiry. No cross-program edge. |
| [#112](https://github.com/the-sarge/quic-go-fast/issues/112) engine contract feasibility | Research of application admission, feedback, cancellation and TLS ownership. No implementation baseline or adopted engine policy. None of the current fixes requires its outcome. |
| [#113](https://github.com/the-sarge/quic-go-fast/issues/113) storage alternatives | Research compares reliable-stream ownership and DATAGRAM leases/batches. Current STREAM callback disposal preserves existing copied frame representation and public lifetime. Do not adopt leases/chunks or treat the research as a prerequisite. |
| [#114](https://github.com/the-sarge/quic-go-fast/issues/114) deployment facts | Needed for workload-sensitive future choices, not correctness-preserving cleanup. Do not infer new workloads or make a fresh performance claim. |
| [#115](https://github.com/the-sarge/quic-go-fast/issues/115) engine comparison design | Already depends on #112 for its own experiment proposal. Do not transplant that edge into this program or run its proposed experiment. |

### Relevant branches and merged history

Branch ancestry alone cannot identify completion after squash merges. Inspected branch tips, relevant diffs, current plans and merged history rather than importing any unmerged tip.

- `codex/i6-server-admission` is prior root QUIC server input/response queue admission and drain work. Current incoming-lifetime plan marks I6 complete. Its owner is the root server enqueue barrier and producer/response-worker exits; the new HTTP/3 proposal owns connection count reservation and HTTP server shutdown completion. Sharing the word admission is not an overlap in central enforcement seam.
- `codex/i7-raw-reader-ownership` is prior raw-reader cache retirement; I7 is complete. The accepted shared coalesced delivery operation preserves that responsibility and keeps native Unix unread batch cleanup separate. It is a new consolidation, not I7 recertification.
- `origin/codex/g1-coalesced-storage`, `codex/g2-linux-gro-receive`, and Windows W3 are delivered by #236, #239 and #248. The current datapath index explicitly marks G/W/D and the entire program complete with no frontier. Reuse merged `coalesced_slab` and adapter behavior; do not reopen measured adoption budgets or require an unmerged branch. D2 receive batching retired via #259/#260 remains retired.
- `codex/d2-linux-ring` and `codex/d2-paced-head-index` contain older `datagram_queue.go` experiments. Their queue representation differs from the proposed HTTP/3 `state_tracking_stream.go` consumed-slot clearing and the coalesced pending queue. Do not adopt their ring/cursor or performance assumptions as cleanup prerequisites.
- `origin/fix-concurrent-http3-server-serve-and-close` (`a47a1fcc...`, 2022) changes historical listener registration/closed locking. Its patch does not provide current atomic HTTP/3 connection reservation/completion; obsolete branch content is not a substitute for the current source-supported race audit.
- `origin/http3-server-shutdown` (2020) and `origin/http3-server-graceful-shutdown-idle-conns` (`a60c8e...`, 2025) address historical wait/request activity semantics. The latter's patch changes active request notification in `handleConn`, not connection admission reservation. Preserve current merged behavior, no cherry-pick.
- `origin/fix-receive-stream-mutex` (`2c8b939d...`, 2021) is already an ancestor and its tip adds cancellation integration coverage. `origin/workflow-permission-test` (`efe74cdb...`, 2025) is also an ancestor: its apparently relevant pool-return patch actually changes `send_stream.go`. Neither closes the accepted receive-side copied-frame handoff family.
- `origin/draft-ietf-quic-reliable-stream-reset-09` (`93ca3612...`, July 2026) changes frame/transport-parameter protocol identifiers and documentation, not the receive cancel wakeup guard. Do not upgrade protocol support through this wakeup fix.

### Precise-root comparison

No automatic repeated-root stop is established. The required comparison yields different invariants/owners rather than a repeated accepted family:

| Existing case / owner | Proposed case / owner | Why not one precise root |
| --- | --- | --- |
| #134 timeout wrapper must join I/O/watchdog before returning caller buffer; local helper wrapper | Local `ReceiveStream.CancelRead` must wake a Read/Peek waiter after reliable-prefix reset; `cancelReadImpl` notification | Caller buffer-return joining and internal receive wakeup are independently owned; #134's wrapper correction did not promise all runtime cancellation transitions. |
| I6 root server enqueue must seal before input/response queue drains; root admission/run workers | HTTP/3 count reservation must be atomic with seal and stable zero-completion; `http3.Server` mutex/count/done operations | Different objects, ownership domains, producer barriers and counted work. Broad lifecycle/admission similarity is insufficient. |
| I2/I3 UDP packet retention/terminal release; received-packet owners | Pooled owning `wire.StreamFrame` must transfer/dispose through parser/dispatch/sorter | STREAM storage is a copied frame allocation, not the incoming UDP slab. The prior accepted packet representation did not encompass this frame-pool lifetime. |
| #149 batch record/member admission against capture finalization; recorder mutex | Sibling packet refs initialized before first runtime publication; concrete delivery queue/slab | Diagnostic record group atomicity and runtime reference lifetime have different domains and central seams. |
| Historical #44/#131/#124 handshake completion assertions; causal owner unknown | Nine source-supported cleanup/refactor obligations | An assertion location or timeout symptom is not a causal central enforcement owner. No prior fix is known that had to cover a later semantic sibling. |

## Actual docs publication path

The pinned repository has no `ci.yml`, no Taskfile preflight and no `ci-*` job names. `lint.yml`, `unit.yml`, `integration.yml`, and `cross-compile.yml` each use `on: [push, pull_request]`, with no draft or docs filter. Interop Docker contributes the remaining push check. Unit and integration include native Ubuntu/Windows/macOS plus existing Linux race runs; cross-compile spans supported Go targets. No need to invent a portfolio migration. API inspection returned no rulesets and main branch protection HTTP 404; lack of server enforcement does not waive the skill's review/certification requirements.

Concrete recent receipts: architecture-handoff docs [PR #224](https://github.com/the-sarge/quic-go-fast/pull/224) published accepted considered/verified plans with source head `47fd4853...` and merged plan SHA `cc7d63f845da1e6cd71a5503083c79a14fbb69b6` before issue publication. Docs-only disposition [PR #260](https://github.com/the-sarge/quic-go-fast/pull/260) ran a briefed RAS review; five low docs fixes were independently resolved with local checks and fresh local certification, explicitly skipping another RAS verify/re-review under the shared low/nit policy. Latest journal [PR #264](https://github.com/the-sarge/quic-go-fast/pull/264) records append-only/format checks, no RAS for the journal, and all 33 hosted checks successful at `54344a161289b6afbc7ee3b31d92df9d2311f274` before merge `fdb1af2...`.

Apply the handoff skill's audit/review requirement and shared bounded review policies to substantive plan docs; use lightweight structural/link/contract checks as local docs certification, then all applicable ordinary exact-head hosted checks. The scoped repository convention in `docs/agents/optional-tracing-ownership.md:20` confirms exact live head, mark ready after local checks, and squash merge guarded by the pinned head under these upstream-style workflows. It is evidence of the real CI adapter, not permission to copy unrelated tracing test requirements into this program. A new repository test failure still requires diagnosis and disposition; old issue-specific admin merges and rerun exceptions are not standing waivers. Issue and OmniFocus publication must wait for the verified reachable default-branch plan commit.
