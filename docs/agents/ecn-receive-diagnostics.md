# ECN receive diagnostics (#528)

## Current contract

The user approved one diagnostics PR and a bounded investigation of [#528](https://github.com/the-sarge/quic-go-fast/issues/528#issuecomment-5789313602). Receiver errors must reach the test with error identity and phase/listener/sender/destination context; a genuine no-delivery timeout still fails. Preserve the existing IPv4 and IPv6 real-socket delivery, source-address and ECN assertions and each existing timeout. The worker must stop before its buffers are released, without reporting from the worker or after the test. Publish every replay outcome and classify the diagnosis as supported, narrowed or unresolved. Diagnostics and passing replays do not establish a fix. If unresolved, keep both the issue and OmniFocus task open with the next missing observation.

The implementation context is this contract, the issue's acceptance criteria, `sys_conn_oob_test.go`, `sys_conn_oob_receiver_test.go`, the read/cleanup ownership in `sys_conn_oob.go`, and the linked results. No complete historical plan or frozen qualification archive is required. Update current status in place; put historical receipts in the results record.

## Boundary and representation

The approved files are the test receiver, its eight existing test callers, focused regression tests and investigation records. The helper's ordered packet-or-error handoff owns diagnostic delivery; the main test goroutine owns assertions and received packets until shutdown. Cleanup cancels blocked handoffs, closes the socket, joins the reader, and releases reader-owned, received and queued buffers. Go socket/error types and the existing OOB receiver own representation; no protocol parser is added. The guarantee is example-level coverage of the existing real UDP test paths on Darwin, Linux and FreeBSD. Broader contract closure is not triggered: focused tests cover the bounded lifecycle cases without a semantic cross-product or mutation campaign.

Material artifacts: the helper, its tests and socket diagnostics are a maintained verification aid owned by the root-package tests. The operational payoff is distinguishing read/decode termination from missing delivery without altering delivery assertions. Retain this aid while these tests use the receiver helper; retire it when an equivalent replacement owns reporting. This approved diagnostic deliverable gates its own PR, not unrelated shipped behavior. Contracts, replay results, review receipts and task mirrors are process/traceability metadata. No shipped behavior or required production safety enforcement changes.

The blast radius is test-worker ordering, shutdown and buffer lifetime across the eight callers. Ordinary socket setup, public APIs, production receive semantics, dependencies, CI configuration, unrelated helpers/timeouts, Darwin batching admission and frozen evidence are outside scope. No retry-until-green, skips, ignored pre-delivery errors or timeout increases. Any corrective behavior change needs causal evidence, a focused regression and approval of the concrete expanded boundary before code or further RAS work.

## Terminating evidence and budgets

Focused evidence covers error before delivery, packet before terminal error, no delivery, successful delivery followed by cleanup, and cleanup with a pending result. Preserve existing ECN/packet-info tests. Run focused tests and race checks, root-package tests, `go vet .`, `go mod tidy -diff` and whitespace checks. These validate diagnostics, not resolution of the intermittent failure.

Before collection, the matching-host replay budget is at most six invocations or twenty minutes of execution, whichever comes first, with Go 1.26.8 and TIMESCALE_FACTOR=10. Allocate two isolated attempts (60-second test limit), two root-package attempts with original seed 1790116749139531000 (180-second test limit), and two evidence-directed order/shared-state probes. Each invocation uses `-count=1`; no automatic retries. Stop blind repetition if a failure yields an observation to investigate. Record exact source SHA, source differences, commands, OS/build/architecture, Go version, timescale, seed and every outcome. The command runner also imposes a wall-clock limit. Unused probe slots need not be spent without a prediction. Unavailable matching-host access is a limitation, never equivalent evidence from Darwin 27. Local validation and required CI are separate from this incident replay budget and are not expanded into stress campaigns.

The matching reference host discovered from existing evidence is `m4mini`, Darwin 25.6.0 arm64 / macOS 26.6.2 build 25G83. The development host is Darwin 27 arm64. Build the test source and certificate fixtures together on the replay host; do not reuse binaries embedding unavailable absolute fixture paths. A current-source seeded replay is not an exact historical-candidate replay or a recreation of cross-package hosted load.

Review budget: one initial RAS review, verification of independently accepted fixes, at most one replacement review. Use the repository execution overlay: hosted checks run for drafts, there is no `task preflight` or `ci-*` scheme. Certify the exact pushed clean head, require applicable existing hosted checks successful, mark ready and squash merge that head. A changed base requires renewed review/certification/CI. Diagnose failed checks before reruns; only demonstrated external infrastructure failure permits a same-head rerun. Append the dev journal only after merge, without RAS. Preserve branch and stop for an expanded boundary, representation mismatch, repeated precise semantic root or exhausted evidence/review budget that prevents safe completion.

## Results

See the [investigation record](../audits/2026-09-23-ecn-receive-diagnostics/results.md) for source/environment receipts, replay outcomes and the remaining observation. [PR #544](https://github.com/the-sarge/quic-go-fast/pull/544) retains review dispositions and final-head certification.
